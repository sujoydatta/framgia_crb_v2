<% if @user_org.waiting? %>
  <h1><%= t ".invited", name: @user_org.organization_name %></h1>
    <%= form_for @user_org, html: {class: "invite-form"} do |f| %>
      <div class="actions">
        <%= f.submit :accept, class: "btn button btn-submit" %>
      </div>
  <% end %>
<% end %>
